mutation CreateUser($firstName: String!, $lastName: String!, $password: String!, $email: String!, $gender: String!) {
  createUser(firstName: $firstName, lastName: $lastName, email: $email, password: $password, gender: $gender) {
    id
    firstName
    lastName
    email
    gender
  }
}
